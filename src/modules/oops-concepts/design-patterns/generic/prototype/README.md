# Prototype — Generic

Full theory, the shallow/deep copy trap and interview questions:
[`../../backend/prototype/README.md`](../../backend/prototype/README.md)

The classic framing: a standard offer letter template with a contract, benefits, and address block
is cloned per hire and tweaked. Rebuilding the whole thing from scratch each time is the smell that
says a prototype is due.

| Example | Scenario |
| --- | --- |
| `example1` | Cloning an `Employee` template — with the shallow-copy bug next to the correct version |
